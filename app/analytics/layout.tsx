/**
 * Analytics Layout
 * 
 * Wraps analytics pages with dashboard layout
 */

import { ReactNode } from 'react';

export default function AnalyticsLayout({ children }: { children: ReactNode }) {
  return children;
}

/**
 * Assets Layout
 * 
 * Wraps assets pages with dashboard layout for non-admin users
 */

import { ReactNode } from 'react';

export default function AssetsLayout({ children }: { children: ReactNode }) {
  return children;
}

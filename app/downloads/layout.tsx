/**
 * Downloads Layout
 * 
 * Wraps downloads pages with dashboard layout for SEO specialists
 */

import { ReactNode } from 'react';

export default function DownloadsLayout({ children }: { children: ReactNode }) {
  return children;
}
